# Compose Runtime Text Parameter

Verifies that `compose_text_param` concatenates literal text and a connected runtime value into one text parameter.

The fixture models the common filter-predicate shape: a fixed column comparison prefix plus a workflow threshold.
