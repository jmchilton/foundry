# Conditional Run Optional Step

Verifies direct Galaxy `when:` gating from a workflow boolean input.

The workflow has explicit true and false marker paths so tests can assert both branch outcomes without depending on missing-output assertions.
