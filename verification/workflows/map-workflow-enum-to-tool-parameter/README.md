# Map Workflow Enum To Tool Parameter

Verifies that `map_param_value` translates a workflow-facing text choice into a downstream text parameter.

The fixture keeps the output as a workflow parameter so the test checks the mapping step itself, not a later consumer.
